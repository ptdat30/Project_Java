# Cấu hình IDE (IntelliJ IDEA)
# Bỏ qua toàn bộ thư mục .idea/ được tạo bởi IntelliJ IDEA
.idea/
# Bỏ qua các file module của IntelliJ IDEA
*.iml
# Bỏ qua file POM đã được "flatten" (thường do plugin như flatten-maven-plugin tạo ra)
.flattened-pom.xml

# Java compiled/generated files
# Bỏ qua tất cả các file .class (đã biên dịch)
*.class
# Bỏ qua toàn bộ thư mục target/ (chứa kết quả build, JARs, WARs, v.v.)
/target/

# Maven wrapper files
# Bỏ qua thư mục .mvn (chứa các file wrapper)
.mvn/
# Bỏ qua script mvnw (Linux/macOS)
mvnw
# Bỏ qua script mvnw.cmd (Windows)
# Lưu ý: Một số dự án chọn commit các file mvnw/mvnw.cmd/repo của wrapper
# để đảm bảo mọi người dùng cùng phiên bản Maven mà không cần cài đặt.
# Nếu bạn muốn điều đó, hãy bỏ comment các dòng này. Tuy nhiên, phổ biến là ignore chúng.

# Gradle files (nếu bạn sử dụng Gradle thay vì Maven)
#.gradle/
#build/

# IDE specific files (ngoài IntelliJ IDEA)
# Bỏ qua thư mục .vscode/ nếu bạn sử dụng VS Code cho riêng phần BE này
.vscode/

# Logs and Temporary Files
*.log
*.log.* 
*.tmp
# Mở rộng cho các loại file log khác
# debug.log
# error.log
